<div class="container mt-4">
  <h1 class="mb-4">Novo Perfil</h1>

  <div class="card shadow-sm mb-4">
    <div class="card-body">
      <%= render "form", perfil: @perfil %>
    </div>
  </div>

  <%= link_to "Voltar para perfis", perfils_path, class: "btn btn-secondary" %>
</div>
